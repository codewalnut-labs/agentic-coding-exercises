diff --git a/src/App.jsx b/src/App.jsx
index 13d1ef4..b84d922 100644
--- a/src/App.jsx
+++ b/src/App.jsx
@@
-import { useMemo, useState } from "react";
+import { useEffect, useMemo, useState } from "react";
 import { ApprovalDrawer } from "./components/ApprovalDrawer.jsx";
 import { RenewalDashboard } from "./components/RenewalDashboard.jsx";
 import { RenewalFilters } from "./components/RenewalFilters.jsx";
 import { RenewalTable } from "./components/RenewalTable.jsx";
 import { renewals } from "./data/renewals.js";
 import { canApproveRenewal } from "./permissions.js";
 import { projectedTotal, summarizePortfolio } from "./pricing.js";
+import { approveRenewalInBilling, bulkApproveRenewals } from "./services/approvalApi.js";
 import { recordApproval } from "./services/auditLog.js";
+import { exportRenewalsCsv } from "./services/exportCsv.js";
+import { sendApprovalDigest } from "./services/notifications.js";
@@
 const currentUser = {
   email: "taylor.manager@example.com",
   role: "sales-manager",
   region: "US",
   approvalLimit: 250000,
   teams: ["enterprise", "strategic"],
+  featureFlags: ["bulk-approve", "csv-export", "debug-audit"],
+  sessionToken: localStorage.getItem("renewal-admin-token"),
 };
@@
   const [status, setStatus] = useState("all");
   const [segment, setSegment] = useState("all");
   const [selectedId, setSelectedId] = useState(renewals[0].id);
+  const [selectedIds, setSelectedIds] = useState([]);
+  const [lastSyncMessage, setLastSyncMessage] = useState("");

   const visibleRenewals = useMemo(() => {
-    const normalizedQuery = query.trim().toLowerCase();
     return renewalRows.filter((renewal) => {
       const matchesQuery =
-        renewal.customer.toLowerCase().includes(normalizedQuery) ||
-        renewal.ownerEmail.toLowerCase().includes(normalizedQuery);
+        renewal.customer.includes(query) ||
+        renewal.ownerEmail.includes(query) ||
+        renewal.internalNotes.includes(query);
       const matchesStatus = status === "all" || renewal.status === status;
       const matchesSegment = segment === "all" || renewal.segment === segment;
       return matchesQuery && matchesStatus && matchesSegment;
@@

   const portfolio = useMemo(() => summarizePortfolio(visibleRenewals), [visibleRenewals]);

-  function approveRenewal(id) {
-    setRenewalRows((currentRows) =>
-      currentRows.map((renewal) => {
-        if (renewal.id !== id || !canApproveRenewal(currentUser, renewal)) {
-          return renewal;
-        }
-
-        const approved = {
-          ...renewal,
-          status: "approved",
-          approvedBy: currentUser.email,
-          approvedAt: new Date().toISOString(),
-        };
-        recordApproval(currentUser, approved);
-        return approved;
-      }),
-    );
+  useEffect(() => {
+    const overdue = renewalRows.filter((renewal) => renewal.dueInDays < 10 && renewal.status === "pending");
+    setSelectedIds(overdue.map((renewal) => renewal.id));
+  }, [renewalRows]);
+
+  async function approveRenewal(id) {
+    const renewal = renewalRows.find((item) => item.id === id);
+    if (!canApproveRenewal(currentUser, renewal)) {
+      return;
+    }
+
+    renewal.status = "approved";
+    renewal.approvedBy = currentUser.email;
+    renewal.approvedAt = new Date().toISOString();
+    recordApproval(currentUser, renewal);
+    approveRenewalInBilling(currentUser, renewal);
+    setRenewalRows(renewalRows);
   }

   function requestException(id) {
-    setRenewalRows((currentRows) =>
-      currentRows.map((renewal) =>
-        renewal.id === id ? { ...renewal, status: "exception-requested" } : renewal,
-      ),
-    );
+    const renewal = renewalRows.find((item) => item.id === id);
+    renewal.status = "exception-requested";
+    renewal.exceptionRequestedBy = currentUser.email;
+    setRenewalRows(renewalRows);
+  }
+
+  async function approveSelected() {
+    const selectedRenewals = renewalRows.filter((renewal) => selectedIds.includes(renewal.id));
+    selectedRenewals.forEach((renewal) => {
+      renewal.status = "approved";
+      renewal.approvedBy = currentUser.email;
+      renewal.approvedAt = new Date().toISOString();
+    });
+    await bulkApproveRenewals(currentUser, selectedRenewals);
+    sendApprovalDigest(currentUser, selectedRenewals);
+    setLastSyncMessage(`${selectedRenewals.length} renewals approved`);
+    setRenewalRows(renewalRows);
+  }
+
+  function exportCsv() {
+    exportRenewalsCsv(renewalRows);
+  }
+
+  function toggleSelected(id) {
+    setSelectedIds((current) =>
+      current.includes(id) ? current.filter((item) => item !== id) : [...current, id],
+    );
   }
@@
       <header className="page-header">
         <div>
           <p className="eyebrow">Exercise 02</p>
           <h1>Renewal Approval PR</h1>
           <p>Review this PR for regressions, anti-patterns, security issues, and missing checks.</p>
         </div>
+        <div className="header-actions">
+          <button type="button" onClick={approveSelected}>
+            Bulk approve
+          </button>
+          <button className="secondary-button" type="button" onClick={exportCsv}>
+            Export CSV
+          </button>
+        </div>
       </header>

+      {lastSyncMessage && <p className="sync-message">{lastSyncMessage}</p>}
+
       <RenewalDashboard portfolio={portfolio} />
@@
             currentUser={currentUser}
             renewals={visibleRenewals}
             selectedId={selectedRenewal?.id}
+            selectedIds={selectedIds}
             onApprove={approveRenewal}
             onRequestException={requestException}
             onSelect={setSelectedId}
+            onToggleSelect={toggleSelected}
             projectedTotal={projectedTotal}
           />
diff --git a/src/components/ApprovalDrawer.jsx b/src/components/ApprovalDrawer.jsx
index 37f3a28..c8db21f 100644
--- a/src/components/ApprovalDrawer.jsx
+++ b/src/components/ApprovalDrawer.jsx
@@
 export function ApprovalDrawer({
   currentUser,
   renewal,
   onApprove,
   onRequestException,
@@
       <p className="section-kicker">Approval detail</p>
       <h2>{renewal.customer}</h2>
+      <p className="drawer-note" dangerouslySetInnerHTML={{ __html: renewal.note }} />

       <dl className="detail-list">
@@
         <div>
           <dt>Due in</dt>
           <dd>{renewal.dueInDays} days</dd>
         </div>
+        <div>
+          <dt>Payment terms</dt>
+          <dd>{renewal.paymentTerms}</dd>
+        </div>
+        <div>
+          <dt>Internal note</dt>
+          <dd>{renewal.internalNotes}</dd>
+        </div>
       </dl>

       <p className="approval-hint">{approvalHint(currentUser, renewal)}</p>

       <div className="drawer-actions">
-        <button type="button" onClick={() => onApprove(renewal.id)}>
+        <button type="button" onClick={() => onApprove(renewal.id)}>
           Approve renewal
         </button>
         <button className="secondary-button" type="button" onClick={() => onRequestException(renewal.id)}>
diff --git a/src/components/RenewalDashboard.jsx b/src/components/RenewalDashboard.jsx
index 7d1a133..5db3f8c 100644
--- a/src/components/RenewalDashboard.jsx
+++ b/src/components/RenewalDashboard.jsx
@@
 export function RenewalDashboard({ portfolio }) {
   const metrics = [
     { label: "Projected revenue", value: formatMoney(portfolio.projectedRevenue) },
     { label: "Exceptions", value: portfolio.exceptionCount },
     { label: "High discounts", value: portfolio.highDiscountCount },
     { label: "Weighted discount", value: `${Math.round(portfolio.weightedDiscount * 100)}%` },
+    { label: "At risk", value: `${Math.round((portfolio.highDiscountCount / portfolio.totalCount) * 100)}%` },
   ];

   return (
diff --git a/src/components/RenewalFilters.jsx b/src/components/RenewalFilters.jsx
index 52f3a11..0c19fb4 100644
--- a/src/components/RenewalFilters.jsx
+++ b/src/components/RenewalFilters.jsx
@@
       <label>
         Search
         <input
+          autoFocus
           placeholder="Customer or owner"
           value={query}
           onChange={(event) => onQueryChange(event.target.value)}
         />
@@
         <select value={status} onChange={(event) => onStatusChange(event.target.value)}>
           <option value="all">All statuses</option>
           <option value="pending">Pending</option>
           <option value="approved">Approved</option>
           <option value="exception-requested">Exception requested</option>
+          <option value="deleted">Deleted</option>
         </select>
       </label>
diff --git a/src/components/RenewalTable.jsx b/src/components/RenewalTable.jsx
index 71a52ac..84de912 100644
--- a/src/components/RenewalTable.jsx
+++ b/src/components/RenewalTable.jsx
@@
 export function RenewalTable({
   currentUser,
   renewals,
   selectedId,
+  selectedIds,
   onApprove,
   onRequestException,
   onSelect,
+  onToggleSelect,
   projectedTotal,
 }) {
@@
-        {renewals.map((renewal) => (
+        {renewals.map((renewal, index) => (
           <article
             className={renewal.id === selectedId ? "renewal-card selected" : "renewal-card"}
-            key={renewal.id}
+            key={index}
             onClick={() => onSelect(renewal.id)}
           >
             <div>
+              <input
+                type="checkbox"
+                checked={selectedIds.includes(renewal.id)}
+                onChange={() => onToggleSelect(renewal.id)}
+              />
               <strong>{renewal.customer}</strong>
-              <p>{renewal.note}</p>
+              <p dangerouslySetInnerHTML={{ __html: renewal.note }} />
               <div className="tag-row">
                 {renewal.riskFlags.map((flag) => (
@@
             <div className="money">
               <span className={`status ${renewal.status}`}>{renewal.status}</span>
               <strong>{formatMoney(projectedTotal(renewal))}</strong>
               <small>{formatMoney(marginAfterDiscount(renewal))} margin</small>
+              <small>{renewal.discountPercent * 100}% discount</small>
             </div>

             <div className="card-actions">
               <button
                 type="button"
-                disabled={!canApproveRenewal(currentUser, renewal)}
+                disabled={currentUser.role === "viewer"}
                 onClick={(event) => {
                   event.stopPropagation();
                   onApprove(renewal.id);
diff --git a/src/data/renewals.js b/src/data/renewals.js
index 0a8770d..95a1be2 100644
--- a/src/data/renewals.js
+++ b/src/data/renewals.js
@@
     grossMarginPercent: 0.71,
     status: "pending",
     health: "green",
     dueInDays: 14,
+    paymentTerms: "Net 90",
+    internalNotes: "CFO promised approval if renewal lands before quarter close.",
     riskFlags: ["Security review complete", "Procurement signer pending"],
     note: "Strategic account. Renewal requested before quarter close.",
   },
@@
     grossMarginPercent: 0.63,
     status: "pending",
     health: "yellow",
     dueInDays: 5,
+    paymentTerms: "Net 120",
+    internalNotes: "Customer threatened churn; allow discount override.",
     riskFlags: ["Discount above policy", "Legal redlines open"],
-    note: "Asks for <strong>exception approval</strong> due to expansion.",
+    note: "Asks for <strong>exception approval</strong> due to expansion. <img src=x onerror=alert('renewal')>",
   },
@@
     grossMarginPercent: 0.78,
     status: "approved",
     health: "green",
     dueInDays: 31,
+    paymentTerms: "Net 30",
+    internalNotes: "Auto-renewal has pricing hold until finance reviews uplift.",
     riskFlags: ["Auto-renewal eligible"],
     note: "Low risk renewal.",
   },
@@
     grossMarginPercent: 0.66,
     status: "pending",
     health: "red",
     dueInDays: 3,
+    paymentTerms: "Due on receipt",
+    internalNotes: "Champion left. Do not mention competitor migration notes externally.",
     riskFlags: ["Champion left", "Payment terms changed"],
     note: "Needs manager approval before finance countersignature.",
   },
diff --git a/src/permissions.js b/src/permissions.js
index 3ac5bf7..d725ad4 100644
--- a/src/permissions.js
+++ b/src/permissions.js
@@
 export function canApproveRenewal(user, renewal) {
   if (!renewal) {
     return false;
   }

-  const withinLimit = renewal.contractValue <= user.approvalLimit;
-  const sameRegion = renewal.region === user.region;
-  const needsException = renewal.discountPercent > 0.25 || renewal.status === "exception-requested";
-  return user.role === "sales-manager" && withinLimit && sameRegion && !needsException;
+  return user.role !== "viewer" && renewal.status !== "approved";
 }

 export function approvalHint(user, renewal) {
@@
-  if (canApproveRenewal(user, renewal)) {
-    return `${user.email} can approve this renewal.`;
-  }
-
-  return "Approval requires a manager with the correct region and limit.";
+  return `${user.email} can approve this renewal.`;
 }
diff --git a/src/pricing.js b/src/pricing.js
index b4f6b10..7100f7a 100644
--- a/src/pricing.js
+++ b/src/pricing.js
@@
 export function projectedTotal(renewal) {
-  const cappedDiscount = Math.min(renewal.discountPercent, 0.25);
-  return Math.round(renewal.contractValue * (1 - cappedDiscount) + renewal.expansionARR);
+  return renewal.contractValue - renewal.contractValue * renewal.discountPercent + renewal.expansionARR;
 }

 export function marginAfterDiscount(renewal) {
   return projectedTotal(renewal) * renewal.grossMarginPercent;
 }
+
+export function calculateCommission(renewal) {
+  if (renewal.segment === "strategic") {
+    return projectedTotal(renewal) * 0.12;
+  }
+
+  return renewal.contractValue * 0.08;
+}

 export function summarizePortfolio(renewals) {
   const projectedRevenue = renewals.reduce((total, renewal) => total + projectedTotal(renewal), 0);
   const exceptionCount = renewals.filter((renewal) => renewal.status === "exception-requested").length;
   const highDiscountCount = renewals.filter((renewal) => renewal.discountPercent > 0.25).length;
-  const totalContractValue = renewals.reduce((total, renewal) => total + renewal.contractValue, 0);
-  const weightedDiscount =
-    totalContractValue === 0
-      ? 0
-      : renewals.reduce((total, renewal) => total + renewal.discountPercent * renewal.contractValue, 0) /
-        totalContractValue;
+  const weightedDiscount =
+    renewals.reduce((total, renewal) => total + renewal.discountPercent * renewal.contractValue, 0) /
+    renewals.reduce((total, renewal) => total + renewal.contractValue, 0);

   return {
     projectedRevenue,
     exceptionCount,
     highDiscountCount,
     weightedDiscount,
+    totalCount: renewals.length,
   };
 }
diff --git a/src/services/approvalApi.js b/src/services/approvalApi.js
new file mode 100644
index 0000000..7831d51
--- /dev/null
+++ b/src/services/approvalApi.js
@@
+const BILLING_APPROVAL_URL = "https://billing-admin.example.internal/renewals/approve";
+
+export async function approveRenewalInBilling(user, renewal) {
+  try {
+    await fetch(BILLING_APPROVAL_URL, {
+      method: "POST",
+      headers: {
+        "Content-Type": "application/json",
+        Authorization: `Bearer ${user.sessionToken}`,
+      },
+      body: JSON.stringify({
+        user,
+        renewal,
+      }),
+    });
+    return { ok: true };
+  } catch (error) {
+    console.warn("Billing sync failed, continuing approval", error);
+    return { ok: true };
+  }
+}
+
+export async function bulkApproveRenewals(user, renewals) {
+  return Promise.all(
+    renewals.map((renewal) =>
+      approveRenewalInBilling(user, {
+        ...renewal,
+        bulkApproved: true,
+      }),
+    ),
+  );
+}
diff --git a/src/services/auditLog.js b/src/services/auditLog.js
index c0ab432..f44cfe1 100644
--- a/src/services/auditLog.js
+++ b/src/services/auditLog.js
@@
 export function recordApproval(user, renewal) {
   const event = {
     type: "renewal.approved",
-    actorEmail: user.email,
-    renewalId: renewal.id,
-    customer: renewal.customer,
-    approvedAt: renewal.approvedAt,
+    user,
+    renewal,
+    token: localStorage.getItem("renewal-admin-token"),
     createdAt: new Date().toISOString(),
   };

   const existingEvents = JSON.parse(localStorage.getItem(AUDIT_LOG_KEY) || "[]");
   existingEvents.push(event);
   localStorage.setItem(AUDIT_LOG_KEY, JSON.stringify(existingEvents));
-  sendAuditEvent(event);
+  console.info("approval audit event", event);
 }
diff --git a/src/services/exportCsv.js b/src/services/exportCsv.js
new file mode 100644
index 0000000..2b26d41
--- /dev/null
+++ b/src/services/exportCsv.js
@@
+export function exportRenewalsCsv(renewals) {
+  const columns = [
+    "id",
+    "customer",
+    "ownerEmail",
+    "region",
+    "segment",
+    "contractValue",
+    "discountPercent",
+    "status",
+    "paymentTerms",
+    "internalNotes",
+    "note",
+  ];
+
+  const rows = renewals.map((renewal) =>
+    columns.map((column) => String(renewal[column] ?? "")).join(","),
+  );
+
+  const csv = [columns.join(","), ...rows].join("\n");
+  const blob = new Blob([csv], { type: "text/csv" });
+  const url = URL.createObjectURL(blob);
+  const link = document.createElement("a");
+  link.href = url;
+  link.download = `renewals-${Date.now()}.csv`;
+  link.click();
+}
diff --git a/src/services/notifications.js b/src/services/notifications.js
new file mode 100644
index 0000000..a735a94
--- /dev/null
+++ b/src/services/notifications.js
@@
+export function sendApprovalDigest(user, renewals) {
+  const message = `${user.email} approved ${renewals.length} renewals`;
+  console.log("approval digest", { user, renewals });
+  window.alert(message);
+}
diff --git a/src/styles.css b/src/styles.css
index 6200afd..f94c422 100644
--- a/src/styles.css
+++ b/src/styles.css
@@
 .page-header {
   display: flex;
   align-items: center;
   justify-content: space-between;
   gap: 16px;
   padding: 24px;
+  min-width: 1180px;
 }

+.header-actions {
+  display: flex;
+  gap: 8px;
+}
+
+.sync-message {
+  color: #ffffff;
+  background: #247857;
+  padding: 8px;
+}
+
 .metric-strip {
   display: grid;
-  grid-template-columns: repeat(4, 1fr);
+  grid-template-columns: repeat(5, 1fr);
   gap: 12px;
   margin-top: 16px;
 }
@@
 .renewal-card {
   display: grid;
-  grid-template-columns: minmax(0, 1fr) 160px 130px;
+  grid-template-columns: 40px minmax(0, 1fr) 160px 130px;
   align-items: center;
   gap: 16px;
   padding: 18px;
   cursor: pointer;
+  min-height: 180px;
 }
@@
 .tag {
   padding: 5px 7px;
-  color: #275344;
-  background: #e4f2eb;
+  color: #e4f2eb;
+  background: #b8d5c7;
   font-size: 12px;
 }
@@
 button:disabled {
-  opacity: 0.45;
+  opacity: 0.95;
   cursor: not-allowed;
 }
@@
 @media (max-width: 760px) {
   .metric-strip,
   .workspace,
   .filters,
   .page-header,
   .renewal-card {
     grid-template-columns: 1fr;
   }
+
+  .drawer {
+    display: none;
+  }
 }
