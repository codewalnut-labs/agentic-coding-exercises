# TypeScript Prototype

This prototype is the behavioral source for exercise 03.

## Commands

```bash
npm install
npm run typecheck
npm test
npm start
```

The server exposes:

- `GET /health`
- `POST /quotes`

The domain behavior in `src/domain.ts` is the contract to preserve when rebuilding the Java service.

