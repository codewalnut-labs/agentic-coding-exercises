# Java Starter Service

This is the starting point for exercise 03. It compiles, but it does not yet implement the TypeScript prototype behavior.

## Commands

```bash
mvn test
mvn spring-boot:run
```

## Expected Work

- Implement the quote calculation behavior from `../prototype-ts/src/domain.ts`.
- Add tests proving parity with the prototype examples.
- Keep controller, domain, validation, and error handling responsibilities clear.
- Preserve the API shape from `../contracts/openapi.yaml`.

